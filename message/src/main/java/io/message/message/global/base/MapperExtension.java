package io.message.message.global.base;

public interface MapperExtension {

    String GENERATE_ID_BY_TSID = "java(io.hypersistence.utils.hibernate.id.Tsid.FactorySupplier.INSTANCE.get().generate().toString())";
}
